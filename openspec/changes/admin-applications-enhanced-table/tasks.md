## 1. 后端 API 增强 - 排序和筛选参数支持

- [x] 1.1 扩展 `GET /api/applications` 接口，添加 `sort_by` 和 `sort_order` 查询参数解析
- [x] 1.2 实现排序字段白名单校验（`created_at`, `full_number`, `applicant_name`）
- [x] 1.3 实现排序 SQL 拼接，支持 `ORDER BY ${sort_by} ${sort_order}`
- [x] 1.4 扩展现有筛选参数支持（确保 `applicant_name`, `ip_address`, `start_date`, `end_date` 已支持）
- [ ] 1.5 编写后端排序和筛选参数的单元测试

## 2. 前端 - 多字段筛选功能

- [x] 2.1 创建筛选状态管理 Hook，使用 `useSearchParams` 同步 URL
- [x] 2.2 添加申请人姓名筛选输入框
- [x] 2.3 添加项目代号下拉选择器（复用 `FilterableProjectSelector` 或简化版）
- [x] 2.4 添加编号类型下拉选择器
- [x] 2.5 添加 IP 地址筛选输入框
- [x] 2.6 添加日期范围筛选组件（两个 `Input type="date"`）
- [x] 2.7 实现筛选条件标签显示（Badge + 关闭按钮）
- [x] 2.8 实现"清除所有筛选"按钮
- [x] 2.9 实现从 URL 参数加载筛选状态（页面初始化）
- [x] 2.10 更新 `applicationAPI.getAll` 调用，传递所有筛选参数

## 3. 前端 - 表格列排序功能

- [x] 3.1 创建排序状态管理 Hook（`sort_by`, `sort_order`）
- [x] 3.2 实现可点击的表头组件（支持三种状态：升序/降序/无排序）
- [x] 3.3 为"申请时间"列添加排序功能
- [x] 3.4 为"完整编号"列添加排序功能
- [x] 3.5 为"申请人"列添加排序功能
- [x] 3.6 添加排序图标（↑↓↕）和样式
- [x] 3.7 更新 API 调用，传递 `sort_by` 和 `sort_order` 参数
- [x] 3.8 确保分页切换时保持排序状态

## 4. 前端 - 多选和批量删除功能

- [x] 4.1 为表格每行添加复选框列（最左侧）
- [x] 4.2 实现表头"全选/反选"复选框
- [x] 4.3 实现选中状态管理（React state 数组存储选中 ID）
- [x] 4.4 实现选中计数显示（"已选择 N 项"）
- [x] 4.5 创建浮动操作栏组件（选中时显示）
- [x] 4.6 在浮动操作栏添加"批量删除"按钮
- [x] 4.7 实现批量删除确认对话框（显示选中数量）
- [x] 4.8 调用现有 `adminAPI.batchDeleteApplications` 接口
- [x] 4.9 处理批量删除结果（成功提示、错误处理、刷新表格）
- [x] 4.10 实现筛选/分页变化时清空选中状态

## 5. UI 优化和样式调整

- [x] 5.1 优化筛选区域布局和间距
- [x] 5.2 添加筛选区域边框和背景色
- [x] 5.3 优化表格列宽和对齐
- [x] 5.4 添加排序列的悬停效果
- [x] 5.5 优化浮动操作栏样式和动画
- [x] 5.6 确保移动端响应式布局正常

## 6. 测试和验证

- [x] 6.1 测试多字段组合筛选功能
- [x] 6.2 测试筛选条件标签的单独清除功能
- [x] 6.3 测试 URL 书签加载筛选状态
- [x] 6.4 测试三态排序切换（升序→降序→无排序）
- [x] 6.5 测试排序与筛选的组合
- [x] 6.6 测试全选/反选/单选功能
- [x] 6.7 测试批量删除完整流程（含确认对话框）
- [x] 6.8 测试批量删除部分失败的处理
- [x] 6.9 测试分页切换时选中状态清空
- [x] 6.10 运行前端 lint 和类型检查（`npm run lint && tsc --noEmit`）
