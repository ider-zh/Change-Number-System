## 1. Update tab title

- [x] 1.1 Open `frontend/index.html` and locate the `<title>` tag in the `<head>` section
- [x] 1.2 Change `<title>frontend</title>` to `<title>自动取号系统</title>`
- [x] 1.3 Verify the change by opening the application in a browser and confirming the tab shows "自动取号系统"
