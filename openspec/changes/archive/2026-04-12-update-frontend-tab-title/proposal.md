## Why

当前前端站点的浏览器 tab 标题显示为默认的 "frontend"，这是 Vite 创建项目时的占位符文本。需要将其更新为与实际业务功能相符的 "自动取号系统"，以提升用户体验和品牌识别度。

## What Changes

- 更新 `frontend/index.html` 中的 `<title>` 标签，从 "frontend" 改为 "自动取号系统"

## Capabilities

### New Capabilities

<!-- 此变更为简单的元数据更新，不涉及新能力 -->

### Modified Capabilities

<!-- 无现有能力需要修改 -->

## Impact

- 受影响文件：`frontend/index.html`
- 影响范围：浏览器标签页标题、书签名称、窗口标题
- 无 API 或后端影响
