## ADDED Requirements

### Requirement: Browser tab title displays "自动取号系统"

The frontend application SHALL display "自动取号系统" as the browser tab title.

#### Scenario: Page loads in browser
- **WHEN** user opens the frontend application in a browser
- **THEN** the browser tab shows "自动取号系统" as the page title
